import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.translator.client',
  appName: 'Translator Client',
  webDir: 'dist'
};

export default config;
